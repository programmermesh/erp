import { MarketPotentialsFileEntity } from './market-potentials-file.entity';

describe('MarketPotentialsFileEntity', () => {
  it('should be defined', () => {
    expect(new MarketPotentialsFileEntity()).toBeDefined();
  });
});
