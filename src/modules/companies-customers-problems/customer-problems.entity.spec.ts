import { CustomerProblemsEntity } from './customer-problems.entity';

describe('CustomerProblemsEntity', () => {
  it('should be defined', () => {
    expect(new CustomerProblemsEntity()).toBeDefined();
  });
});
