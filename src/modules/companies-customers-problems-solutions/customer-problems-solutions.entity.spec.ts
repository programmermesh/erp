import { CustomerProblemsSolutionsEntity } from './customer-problems-solutions.entity';

describe('CustomerProblemsSolutionsEntity', () => {
  it('should be defined', () => {
    expect(new CustomerProblemsSolutionsEntity()).toBeDefined();
  });
});
