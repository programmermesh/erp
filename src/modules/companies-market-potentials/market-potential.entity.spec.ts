import { MarketPotentialEntity } from './market-potential.entity';

describe('MarketPotentialEntity', () => {
  it('should be defined', () => {
    expect(new MarketPotentialEntity()).toBeDefined();
  });
});
