import { ChannelDetailEntity } from './channel-detail.entity';

describe('ChannelDetailEntity', () => {
  it('should be defined', () => {
    expect(new ChannelDetailEntity()).toBeDefined();
  });
});
