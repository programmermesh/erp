import { CompanyRelationEntity } from './company-relation.entity';

describe('CompanyRelationEntity', () => {
  it('should be defined', () => {
    expect(new CompanyRelationEntity()).toBeDefined();
  });
});
