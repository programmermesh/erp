import { RiskAssessmentEntity } from './risk-assessment.entity';

describe('RiskAssessmentEntity', () => {
  it('should be defined', () => {
    expect(new RiskAssessmentEntity()).toBeDefined();
  });
});
