import { CustomerSegTypeSubcategoryEntity } from './customer-seg-type-subcategory.entity';

describe('CustomerSegTypeSubcategoryEntity', () => {
  it('should be defined', () => {
    expect(new CustomerSegTypeSubcategoryEntity()).toBeDefined();
  });
});
