import { ConnectionGroupsLeadListEntity } from './connection-groups-lead-list.entity';

describe('ConnectionGroupsLeadListEntity', () => {
  it('should be defined', () => {
    expect(new ConnectionGroupsLeadListEntity()).toBeDefined();
  });
});
