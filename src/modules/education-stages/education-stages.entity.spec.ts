import { EducationStagesEntity } from './education-stages.entity';

describe('EducationStagesEntity', () => {
  it('should be defined', () => {
    expect(new EducationStagesEntity()).toBeDefined();
  });
});
