import { CompanyNetworksEntity } from './company-networks.entity';

describe('CompanyNetworksEntity', () => {
  it('should be defined', () => {
    expect(new CompanyNetworksEntity()).toBeDefined();
  });
});
