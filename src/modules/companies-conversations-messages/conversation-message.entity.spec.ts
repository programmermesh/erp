import { ConversationMessageEntity } from './conversation-message.entity';

describe('ConversationMessageEntity', () => {
  it('should be defined', () => {
    expect(new ConversationMessageEntity()).toBeDefined();
  });
});
