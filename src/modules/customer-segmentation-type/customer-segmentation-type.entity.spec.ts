import { CustomerSegmentationTypeEntity } from './customer-segmentation-type.entity';

describe('CustomerSegmentationTypeEntity', () => {
  it('should be defined', () => {
    expect(new CustomerSegmentationTypeEntity()).toBeDefined();
  });
});
