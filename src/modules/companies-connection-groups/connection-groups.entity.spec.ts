import { ConnectionGroupsEntity } from './connection-groups.entity';

describe('ConnectionGroupsEntity', () => {
  it('should be defined', () => {
    expect(new ConnectionGroupsEntity()).toBeDefined();
  });
});
